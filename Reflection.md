# ICA04 Reflection

## 1. Local and Remote Repositories

What is the difference between the local TaskTrack repository and the repository hosted on GitHub?
the main difference is how to store a file.
Local tasktrack repository is a copy of your file is stored on your computer so that you can work on your project you can edit, write code, test code,and check the changes with git status.
Repository hosted on GitHun is the where we stored our copy of local repository file online for the purpose of sharing code with group, to see what piece of code have been changed.

## 2. Connecting and Pushing

Why did adding `origin` not immediately place the project files on GitHub?
adding origin is like telling git the online GitHub repository for this project is called Origin

## 3. Cloning

How is cloning a repository different from downloading its files as a ZIP archive?
The main difference is that cloning creates a Git repository on your computer, while downloading a ZIP just gives you a copy of the files.

## 4. Fetching and Pulling

What information did `git fetch` update, and what additional action did `git pull` perform?
The main difference between those term is one for showing the changes and others git those changes from Github and apply them to the local repository.

## 5. Focused Commits

Why is it useful to commit the Python feature, sample task data, and README documentation separately?
because each commit represent one clear change, easy to understand by group member, easier to find the problem, the best way to collaborate with the team.
